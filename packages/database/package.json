{
  "name": "@flowhub/database",
  "version": "0.1.0",
  "private": true,
  "description": "Drizzle ORM schema, migrations and DB client for FlowHub AI (implemented in Cycle 4)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "node --test dist/",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@flowhub/shared": "workspace:*"
  }
}
