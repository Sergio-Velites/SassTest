# @flowhub/observability

Logging estructurado JSON con sanitización obligatoria (`redact()` enmascara claves tipo token/secret/password a cualquier profundidad). El transporte console se sustituye por pino en el Ciclo 3; la interfaz `Logger` es estable.
