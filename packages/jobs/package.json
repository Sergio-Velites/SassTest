{
  "name": "@flowhub/jobs",
  "version": "0.1.0",
  "private": true,
  "description": "JobQueue abstraction (ADR-0005) with BullMQ and in-memory implementations",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "node --test \"dist/**/*.test.js\"",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "bullmq": "^5.80.8",
    "zod": "^3.24.0"
  },
  "files": [
    "dist"
  ]
}
