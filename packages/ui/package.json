{
  "name": "@flowhub/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared UI components (shadcn/ui based — built out in Cycle 7)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "node --test dist/",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  }
}
