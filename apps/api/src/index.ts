/**
 * apps/api placeholder. Cycle 3 scaffolds the real Fastify server with
 * Zod validation, OpenAPI docs, security headers, CORS and rate limiting;
 * Cycle 5 adds the domain routes. See docs/architecture/ARCHITECTURE.md.
 */
export const APP = 'api';
