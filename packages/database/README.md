# @flowhub/database

Esquema Drizzle ORM, cliente y migraciones. **Estado: pendiente (Ciclo 4).**

El modelo de datos completo está especificado en `docs/architecture/DATA_MODEL.md` — implementar desde esa especificación. Toda tabla tenant-owned lleva `organization_id` y todas las queries filtran por él.
