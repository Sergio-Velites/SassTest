/**
 * apps/web placeholder. Cycle 3 replaces this package with a real Next.js
 * (App Router) application — see docs/product/BACKLOG.md. Kept as a plain
 * TS package for now so the monorepo pipeline (lint/typecheck/build/test)
 * exercises every workspace from day one.
 */
export const APP = 'web';
