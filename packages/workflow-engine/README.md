# @flowhub/workflow-engine

Tipos del contrato de workflows (definición JSON, nodos, edges, estados de ejecución) y validación con Zod. El ejecutor llega en el Ciclo 6.

Especificación completa: `docs/architecture/WORKFLOW_ENGINE.md` (mantener en sync con estos tipos).
