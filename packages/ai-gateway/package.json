{
  "name": "@flowhub/ai-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Provider-agnostic AI layer: interchangeable providers (mock/OpenAI/Anthropic), prompt templates, cost tracking",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "node --test \"dist/**/*.test.js\"",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@flowhub/shared": "workspace:*",
    "zod": "^3.24.0"
  }
}
